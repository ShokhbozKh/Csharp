<UserControl x:Class="FinalProject.Views.SearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinalProject.Views"
             d:DataContext="{d:DesignInstance Type=local:ReservationControl}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <local:RideDetailsControl Grid.Row="0" HorizontalAlignment="Center" Margin="5"/>

        <ListView Name="searchListView" Grid.Row="1" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Operator (Bus type)" Width="140" DisplayMemberBinding="{Binding Bus.BusName}" />
                    <GridViewColumn Header="Dep. Time" Width="100" DisplayMemberBinding="{Binding DepatureTime}" />
                    <GridViewColumn Header="Start point" Width="100" DisplayMemberBinding="{Binding Ride.StartPoint.LocationName}" />
                    <GridViewColumn Header="Destination point" Width="100" DisplayMemberBinding="{Binding Ride.DestinationPoint.LocationName}" />
                    <GridViewColumn Header="Arr. Time" Width="100" DisplayMemberBinding="{Binding ArrivalTime}" />
                    <GridViewColumn Header="Avialable seats" Width="100" DisplayMemberBinding="{Binding ArrivalTime}" />
                    <GridViewColumn Header="Price" Width="100" DisplayMemberBinding="{Binding ArrivalTime}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
